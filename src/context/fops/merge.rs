use crate::{context::fops::QcFileOpsBuilder, error::QcError, prelude::QcContext};

// impl QcContext {
//     /// Merge all RINEX currently indexed by the following description,
//     /// into
//     fn merge_dump_rinex<const M: usize>(&self, fops: QcFileOpsBuilder) -> Result<(), QcError> {}
// }

// use gnss::Merge;

// impl Merge for QcContext {}
